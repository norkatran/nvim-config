return {
  cmd = { "phpactor" },
  filetypes = { "php" },
  root_markers = { "composer.json", "vendor", ".git" },
  telemetry = { enabled = false },
  formatters = {
    ignoreComments = false,
  }
}
